package com.Ultra_Nerd.CodeLyokoLegacy.screens;

public final class QuantumChipletScreen /*extends ContainerScreen*/ {
    /*public QuantumChipletScreen(@NotNull ChestMenu p_98409_, @NotNull Inventory p_98410_, @NotNull Component p_98411_) {
        super(p_98409_, p_98410_, p_98411_);
    }

/*
    public QuantumChipletScreen(QuantumChipletContainer Container, PlayerInventory PI, ITextComponent title) {
        super(Container, PI, title);
        this.guiLeft = 0;
        this.guiTop = 0;
        this.xSize = 176;
        this.ySize = 166;
    }

    private static final ResourceLocation BACK_TEX = new ResourceLocation(CodeLyokoMain.MOD_ID + ":textures/gui/quantumchiplet.png");

    @Override
    public void render(final int p_render_1_, final int p_render_2_, final float p_render_3_) {
        this.renderBackground();
        super.render(p_render_1_, p_render_2_, p_render_3_);
        this.renderHoveredToolTip(p_render_1_, p_render_2_);
    }

    @Override
    protected void drawGuiContainerForegroundLayer(int mouseX, int mouseY) {
        super.drawGuiContainerForegroundLayer(mouseX, mouseY);
        this.font.drawString(this.title.getFormattedText(), 8, 8, 4210752);
        this.font.drawString(this.playerInventory.getDisplayName().getFormattedText(), 8, (float) (this.ySize - 96 + 2), 4210752);
    }

    @Override
    protected void drawGuiContainerBackgroundLayer(float partialTicks, int mouseX, int mouseY) {
        RenderSystem.color4f(1, 1, 1, 1);
        this.minecraft.getTextureManager().bindTexture(BACK_TEX);
        int x = (this.width - this.xSize) / 2;
        int y = (this.height - this.ySize) / 2;
        this.blit(x, y, 0, 0, this.xSize, this.ySize);
    }

 */
}
